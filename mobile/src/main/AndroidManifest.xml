<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required for GATT connection to toggle the load output. Not in :data's manifest because
         the watch app has no use for it — only mobile controls the load. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_victron"
        android:label="${appLabel}"
        android:supportsRtl="true"
        android:theme="@style/Theme.VictronMonitor">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="${appLabel}"
            android:theme="@style/Theme.VictronMonitor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
