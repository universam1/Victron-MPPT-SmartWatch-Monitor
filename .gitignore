.gradle/
build/
local.properties
*.iml
.idea/
.kotlin/
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab
*.keystore
*.jks
keystore.properties
.env
dist/
!gradle/wrapper/gradle-wrapper.jar

# Reverse-engineering artifacts are committed while this repo is private, so the blanket
# *.apk rule above must not swallow the pulled APKs. See tools/victron-re/README.md —
# these must come back OUT of history before the repo is made public again.
!tools/victron-re/work/**

# The extracted native library is 103 MB, over GitHub's 100 MB hard limit, so it stays local.
tools/victron-re/work/lib/
